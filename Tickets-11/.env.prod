# Ambiente PROD (producción)
# Usa Supabase (PostgreSQL)
# Perfil: supabase (mismo que test, pero con valores diferentes)

SPRING_PROFILES_ACTIVE=supabase

# Supabase Configuration
DB_HOST=db.yyyyyyyyyyyy.supabase.co
DB_PORT=5432
DB_NAME=postgres
DB_USER=postgres
DB_PASSWORD=your-prod-password