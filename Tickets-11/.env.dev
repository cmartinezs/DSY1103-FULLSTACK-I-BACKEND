# Ambiente DEV (desarrollo con datos persistentes)
# Usa MySQL local (XAMPP)
# Perfil: mysql

SPRING_PROFILES_ACTIVE=mysql

# MySQL Configuration
DB_HOST=localhost
DB_PORT=3306
DB_NAME=tickets_db
DB_USER=root
DB_PASSWORD=